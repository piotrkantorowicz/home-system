{
  "name": "home-system-release",
  "version": "0.0.0-managed-by-semantic-release",
  "private": true,
  "type": "module",
  "description": "Release tooling only. The app packages live in src/ui and the .NET solution.",
  "scripts": {
    "release": "semantic-release",
    "release:preview": "semantic-release --dry-run --no-ci"
  },
  "devDependencies": {
    "conventional-changelog-conventionalcommits": "^10.4.0",
    "semantic-release": "^25.0.2"
  },
  "engines": {
    "node": ">=24"
  },
  "overrides": {
    "conventional-changelog-writer": "^9.2.1"
  }
}
