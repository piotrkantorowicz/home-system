REPO_ROOT=$(git rev-parse --show-toplevel)

(cd "$REPO_ROOT/src/ui" && npx lint-staged)

# Path-aware checks for whatever is staged — same script the `verify` skill and CI use.
"$REPO_ROOT/scripts/verify.sh" --staged
